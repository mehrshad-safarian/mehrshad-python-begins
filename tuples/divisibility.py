# Check if number is divisible by 2
# number % 2 == 0 # Returns the True if divisible
input_value = int(input("Pls enter a number to check divisibility by 2: "))
print(f"is {input_value} divisible by 2? {input_value % 2 == 0}")